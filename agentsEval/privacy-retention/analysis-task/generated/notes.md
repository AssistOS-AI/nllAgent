# Northstar register materialization notes

The task program materializes six admitted record identities, seven retention declarations, three explicit exception
statements, and seven scope-specific coverage observations. R6 intentionally has two declarations under one identity.
R5 intentionally preserves both documented and undocumented evidence. R7 is an ontology gap anchored to its controlled
line because `unmapped:biometric-template` is outside the trained category vocabulary.

Every claim is explicit and uses the smallest complete controlled line as a half-open Unicode code-point span. The
surrounding business prose is not copied into semantic payloads. Coverage is materialized only where the register has
an explicit controlled coverage line; no document-wide closure is inferred. This program contains no finding or rule
status.
