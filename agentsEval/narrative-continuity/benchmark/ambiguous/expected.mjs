import { narrativeExpectation } from '../expectation.mjs';

export default narrativeExpectation(
  'continuity-ambiguous', 'UNKNOWN', 0,
  'Mara used "the silver case" in the hill tower.',
  'the silver case'
);
