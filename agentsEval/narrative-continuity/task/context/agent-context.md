# Narrative continuity analysis context

Purpose: **ANALYZE**  
Selected agent/build: **narrative-continuity@eval-build-20260731**

The selected materialization profile requires `Leave`, `Retrieve`, `Use`, `DirectBefore`, `CoverageNotice`, exact
actor/object/place/anchor roles, separate actor and object coreference candidates, typed temporal reachability, and
per-use retrieval coverage. The task role may write only `generated/program.mjs`, `notes.md`, and `handoff.md`.

No finding, assessment, new ontology constructor, or trained circuit is authorized in task LongTextJS.
