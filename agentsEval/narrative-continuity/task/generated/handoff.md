# Analysis handoff

Observed concepts: `Leave`, `Retrieve`, `Use`, `DirectBefore`, `NarrativeInterval`, and `CoverageNotice`.

Preserved limitations: unresolved actor/object pronouns, partial compass coverage, unknown pronoun coverage, and a
black-umbrella coverage statement that cannot be attached to one shared object identity.

The host must validate all pinned spans, compile the observation layer, and execute the accepted continuity circuit.
This handoff deliberately contains no verdict.
